[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "tensir"
version = "0.0.1"
description = "QPE run logging infrastructure for quantum chemistry"
readme = "README.md"
requires-python = ">=3.9"
license = {text = "MIT"}